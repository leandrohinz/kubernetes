//jenkinsfile for cd
pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Clone repository') {
            steps {
                git credentialsId: 'git', url: 'https://github.com/leandrohinz/kubernetes.git'
            }
       
        }
    
        stage('Terraform Init') {
            steps {
                sh 'cd /helm-aws/frontend-chart && terraform init'
            }
        }

        stage('Terraform Apply') {
            steps {
                sh 'cd /helm-aws/frontend-chart && terraform apply -auto-approve'
            }
        }
    
    }
}